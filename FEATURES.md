## Accessibility Analysis

* Alt-text checks — flags images missing alternative text
* Heading structure — validates correct heading hierarchy (h1–h6)
* ARIA attribute validation — checks for missing or misused ARIA roles and attributes
* Link text analysis — flags non-descriptive link texts (e.g. "click here")
* Link validation — integrates `cms-linkvalidator` to surface broken links in the accessibility report
